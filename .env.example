# Local development (copy to .env)
VITE_OPENROUTER_API_KEY=
VITE_API_URL=http://localhost:8000/api

# npm run dev        → .env.development (localhost API)
# npm run build:local → .env.development (localhost API, for docker port 3001)
# npm run build      → .env.production (https://api.devnodo.com/api)
