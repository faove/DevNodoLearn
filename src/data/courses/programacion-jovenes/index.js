import {
  courseMeta,
  ranks,
  phaseLabels,
  chapterTitles,
  tagByPhase,
  rankSize,
  maxPhase,
} from './course'
import { lessons } from './lessons'

export default {
  slug: 'programacion-jovenes',
  meta: courseMeta,
  lessons,
  ranks,
  phaseLabels,
  chapterTitles,
  tagByPhase,
  rankSize,
  maxPhase,
}
