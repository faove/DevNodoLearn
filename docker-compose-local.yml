services:
  devnodo-learn:
    image: nginx:alpine
    container_name: devnodo-learn-local
    restart: unless-stopped
    ports:
      - "3001:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
